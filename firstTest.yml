config:
  target: https://httpbin.org/
  phases:
    - duration: 1
      arrivalRate: 1
      name: Arrtilery First Test
scenarios:
  - name: Getting a resource
    flow:
      - get:
          url: "{{ target }}get"